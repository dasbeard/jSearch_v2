import { colors } from './colors';
import { space, spaceInt, lineHeights } from './spacing';
import { sizes, sizesInt } from './sizes';
import { fontWeights, fontSizes } from './fonts';

export const theme = {
  colors,
  space,
  spaceInt,
  lineHeights,
  sizes,
  sizesInt,
  fontSizes,
  fontWeights,
};
